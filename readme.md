# this is my dotfiles

## Installation

```bash
sudo apt-get install git vim

# install homebrew

# install oh-my-zsh

# install tmux
???

# install z.sh
mkdir -p $HOME/config
cp z.sh $HOME/config/
# add "source $HOME/config/z.sh" to your .zshrc
```
