# this is my dotfiles

## Installation

```bash
sudo apt-get install git vim

# install homebrew

# install oh-my-zsh
# copy zshrc ~/.zshrc

# install tmux

# install z.sh
# copy z.sh $HOME/config
```
